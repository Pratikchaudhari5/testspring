services:
  - type: web
    name: ems-backend           # 👈 Your app name (change if you want)
    env: docker                   # 👈 Render will use Java environment
    buildCommand: "mvn clean package -DskipTests"
    startCommand: "java -jar target/ems-backend-0.0.1-SNAPSHOT.jar"
    plan: free                  # 👈 Free Render plan
    envVars:
      - key: PORT
        value: 8080             # 👈 Render auto-overrides this, but keep as default
